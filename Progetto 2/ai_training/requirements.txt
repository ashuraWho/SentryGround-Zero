# Core deep-learning runtime and converter tooling for training and TFLite export.
tensorflow
# Utilities for packing metadata around converted models (optional helpers in some workflows).
tflite-support
# Explainability library used by shap_explainer.py.
shap
# Numerical arrays shared across training, evaluation, and SHAP.
numpy
# Plotting support if you extend scripts to visualize SHAP or reconstructions.
matplotlib
# Metrics such as accuracy_score used in train_model.py evaluation.
scikit-learn
# Headless OpenCV for image pipelines without a GUI (reserved for extensions).
opencv-python-headless
